class ArrayTest {
   /*
    * Tests arrays allocation, index assignments, element access, and length.
    */
    public static void main(String[] a) {
        int[] arr;
        int length;
        arr = new int[5];
        arr[0] = 10;
        arr[1] = 20;
        length = arr.length;
    }
}